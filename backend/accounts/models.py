from django.db import models
from django.contrib.auth.models import AbstractUser

# Extends standard Django user if needed, or uses User model
# The standard auth.User has: id, username, email, password, first_name, last_name
